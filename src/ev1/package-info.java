/**
 * que nos guarde al final del archivo es decir que si lo abrimos para escritura se nos borra los dias anteriores y si existe o no existe usar un modo de apertura u otro.
 * necesitamos que nos guarde las lineas de apertura solo una vez ya que no es necesario repetir informacion.
 * buscar del archivo File como hacer para abrir y que escriba a final del archivo
 * no queremos datos duplicados. p.ej. si o abrimos a las 7pm y luego a las 9pm. hacer que no haya datos duplicados
 */
/**
 * @author Pablo
 *
 */
package ev1;